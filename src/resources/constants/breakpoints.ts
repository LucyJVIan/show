import { lessMediaQuery } from 'resources/utils';

export const device = {
    less1300: lessMediaQuery(1300),
    less980: lessMediaQuery(980),
    less860: lessMediaQuery(860),
    less768: lessMediaQuery(768),
    customLess: lessMediaQuery,
};
